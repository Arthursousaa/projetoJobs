@model List<projetoJobs.Models.Candidato>

<h2>Candidatos para a vaga</h2>

<table class="table table-striped mt-3">
    <thead>
        <tr>
            <th>Nome</th>
            <th>Email</th>
            <th>Telefone</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var c in Model)
        {
            <tr>
                <td>@c.Nome</td>
                <td>@c.Email</td>
                <td>@c.Telefone</td>
            </tr>
        }
    </tbody>
</table>
